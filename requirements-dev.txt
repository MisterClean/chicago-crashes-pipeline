# Optional Development Dependencies
# These packages provide additional functionality but are not required for core operation

# Advanced Testing
types-PyYAML==6.0.12.20240917     # Type stubs for PyYAML (required by mypy)

# CLI and Rich Output
typer==0.9.0             # CLI framework
rich==13.7.0             # Rich console output and formatting

# Spatial Analysis (requires additional system dependencies)
pandas==2.1.3            # Data analysis (requires numpy, may need build tools)
geopandas==0.14.1        # Geospatial data analysis
shapely==2.0.2           # Geometric operations
pyproj==3.6.1            # Cartographic transformations

# Geospatial File Processing
fiona==1.9.5             # Shapefile I/O (requires GDAL)
rasterio==1.3.9          # Raster data I/O (requires GDAL)

# Note: Spatial packages may require additional system dependencies:
# - On macOS: brew install gdal
# - On Ubuntu: apt-get install gdal-bin libgdal-dev
# - On Windows: Install OSGeo4W or use conda
